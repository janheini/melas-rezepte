---
const { tag } = Astro.props;
---

<div>
    <input
        id={tag}
        name="filter"
        value={tag}
        type="checkbox"
        hx-post="/"
        class="peer appearance-none"
    />
    <label
        for={tag}
        class="peer-checked:bg-foreground peer-checked:text-background rounded-full border-2 p-2"
        >{tag}
    </label>
</div>
